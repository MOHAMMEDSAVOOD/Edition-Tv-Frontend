import HomepageCurationClient from "@/components/curation/HomepageCurationClient";

export const metadata = {
  title: "Front-Page Curation | Edition TV CMS",
  description: "Curate lead stories, secondary grids, breaking banners, and editor's picks.",
};

export default function CurationPage() {
  return <HomepageCurationClient />;
}
