import VerificationWorkspaceClient from "@/components/verification/VerificationWorkspaceClient";

export const metadata = {
  title: "Verification & Evidence | Edition TV CMS",
  description: "Primary evidence attachment, claim verification, and source reliability scoring.",
};

export default function VerificationPage() {
  return <VerificationWorkspaceClient />;
}
